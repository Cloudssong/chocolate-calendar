<?php
//---------------------------------------------------Add calendar CSS------------------------------------------------------------------------------

//---------------------------------------------------Change month +/- -----------------------------------------------------------------------------

/* function monthBack( $timestamp ){
    return mktime(0,0,0, date("m",$timestamp)-1,date("d",$timestamp),date("Y",$timestamp) );
}
function yearBack( $timestamp ){
    return mktime(0,0,0, date("m",$timestamp),date("d",$timestamp),date("Y",$timestamp)-1 );
}
function monthForward( $timestamp ){
    return mktime(0,0,0, date("m",$timestamp)+1,date("d",$timestamp),date("Y",$timestamp) );
}
function yearForward( $timestamp ){
    return mktime(0,0,0, date("m",$timestamp),date("d",$timestamp),date("Y",$timestamp)+1 ); 
} */


//------------------------------------------Create list with active days from DB-------------------------------------------------------------------

// Holt uns (button -> on.click) die aktiven Tage und macht eine Liste daraus
/* private function choc-list() {
    $meetings = Choc_Calendar::$current_day[ class = 'active' ] // TODO: Muss aus DB, nicht aus Klasse holen?

    if (// button is clicked) {
        foreach ( $meetings AS $meetings ) {
            // Wirf eine Liste aus mit den Tagen, die aktiv sind
        }
    }
} */

//---------------------------------------------------Navigate to 'today'---------------------------------------------------------------------------



//----------------------------------------------------Show the timeslots---------------------------------------------------------------------------
